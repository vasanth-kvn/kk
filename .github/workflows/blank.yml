name: work-env
on: push
env:
  APP_ENV: "dev"
jobs:
  envs:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: env-workflow-level
        run: echo "taking env from job-level $APP_ENV"
          
  
