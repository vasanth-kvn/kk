name: context
on: push
jobs:
  cont:
    runs-on: ubuntu-latest
    steps:
      - name: context-variable
        run: |
          echo "running by ${{github.actor}}
          echo "to ${{github.repository}}
      - name: all variables
        run: echo ${{toJson(github)}}
        
  
