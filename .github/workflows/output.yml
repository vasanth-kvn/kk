name: if-test-02
on: pull_request
jobs:
  ifcase-2:
    runs-on: ubuntu-latest
    steps:
      - name: if case
        if: github.ref_name =='main'|| github.actor == 'vasanth-kvn1'
        run: echo "run if case work"
  
        
